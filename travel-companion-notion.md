# AI 旅行管家（旅伴）· 项目文档总览

> 维护：谷盼盼（lord）+ 赛巴斯蒂安 · 最近更新 2026-07-13

---

## 一句话

一个**陪你走完每一程、还记得你走过哪儿**的 AI 旅行管家——**行前帮你排，行中陪你走，行后替你记**。
有性格（管家「丸丸」，未来可使用 lord 本人的授权音色）、懂你脾气、记得你。

## 我们和「直接问 AI」的区别（护城河）

通用 AI 是**一个瞬间**（行前问一下就走，不记得你）；我们是**一段关系**（陪完整程 + 把每趟旅行沉淀成记忆）。差异建在四层：**成品化 · 陪伴 · 记忆 · 社交**。完整论证见 [08](08-travel-lifecycle-and-moat.md)。

## 三大模块 × 旅行全周期

| 模块（能力块） | 生命周期（时间轴） | Phase |
|----------------|--------------------|-------|
| ① 行程规划（提问引导式管家） | 行前 · 规划 | **Phase 1（当前）** |
| ③ 中国记忆地图 + 游记 | 行后 · 记忆 | Phase 2 |
| ①延伸 + 实时地图/周边 + 语音陪伴 | 行中 · 陪伴 | Phase 2 起 |
| ② 论坛/朋友圈（社交） | 行后 → 社交 | Phase 3 |

> 飞轮：行中打卡 → 行后游记+点亮地图+反哺偏好 → 管家更懂你+喂养社交 → 行前更准。越转越强、别人复制不了。

## 关键决策（已拍板）

- **技术栈**：React + Vite + TS + Tailwind；前端 → serverless 代理藏 Key → LLM。
- **模型供应商**：**DeepSeek（永久）**，OpenAI 兼容，`deepseek-chat`。当前用 `response_format: json_object` + prompt 描述结构约束输出，并已增加运行时行程结构校验。
- **导出带图**：当前结果页可导出竖版分享卡（PNG）；PDF 暂缓。配图走真实图库/POI，**不用 AI 生成、不爬小红书**。
- **声音**：管家用 **lord 本人音色克隆**（本人授权）。Phase 2/3、需小程序/App。
- **MVP 纪律**：Phase 1 只交付「行前 ①」并打磨到有灵魂，不范围爆炸。

## 文档地图

| 文档 | 内容 |
|------|------|
| [PRD-v0.1](PRD-v0.1.md) | 产品需求：定位、MVP 范围、功能、路线图 |
| [02 竞品分析](02-competitive-analysis.md) | 对手死穴与我们的那条缝 |
| [03 用户故事与流程](03-user-stories-and-flow.md) | 画像、用户故事、流程图 |
| [04 技术架构 + 数据模型](04-architecture-and-data-model.md) | 架构图、行程 JSON 模型、真实性、导出与配图 |
| [05 管家 Prompt 设计](05-butler-prompt-design.md) | 人设、提问引导对话、行程生成 prompt、防编造 |
| [06 管家提问库](06-butler-questions.md) | 提问引导式要问的全部问题（分档） |
| [07 模块与分期](07-product-modules-and-phasing.md) | 三模块、分期上线策略 |
| [08 旅行全周期与护城河](08-travel-lifecycle-and-moat.md) | 行前/行中/行后、记忆飞轮、差异化 |

## 当前进度（2026-07-14）

- ✅ Phase 1 可演示原型已成型：DeepSeek 真生成/修改、Supabase 登录与云端行程、OCR 票务、天气、账本、地图入口、打卡/照片/点评、丸丸成长、PNG 导出。
- ✅ 页面已重构为手机优先的旅行规划工作台：移动端先规划、生成后自动进入结果，带日期快捷导航和底部调整/保存操作；桌面端为规划与行程双栏。登录收进顶部账户面板。
- ✅ TypeScript 检查与 Vite 生产构建通过；GitHub 与 Vercel 发布链路已打通，正式站点可访问。
- ✅ Vercel Production 已切换到有效的 `travel-wanwan` Supabase 项目；认证健康检查和线上登录错误响应已验证，不再出现 `Failed to fetch`。
- ✅ 已完成邮箱/手机密码找回界面、手机号验证码与密码认证流程、Supabase Send SMS Hook、AI 接口登录鉴权、每用户基础限流、请求边界校验、模型返回结构校验和友好错误降级；13 项自动化测试通过。
- ✅ 认证页会自动读取 Supabase Phone Provider 状态：未启用时安全保留邮箱入口，启用后自动开放手机验证码和手机密码。
- ✅ 天气已前置到生成流程；和风天气官方灾害预警已接入，临近出发时会把台风、暴雨等生效预警持久化到行程顶部并约束 DeepSeek 做安全调整；高德事实层代码已接入，配置服务端 Key 后自动核验 POI 和同日相邻路线，异常时让 DeepSeek局部修复。
- ✅ 票据 OCR 在发送模型前自动隐藏姓名、手机号、邮箱和证件号，结果不再保留订单号等无关个人信息。
- ✅ 云端四张业务表、触发器、RLS 和照片桶均已部署；匿名客户端实测全部返回 200。
- ✅ Supabase Site URL 已指向正式站点，密码重置回跳地址已加入允许列表。
- ⚠️ 当前规划入口仍是一段话直接生成，提问引导和偏好记忆尚未闭环。
- ⚠️ 手机认证和腾讯云短信 Hook 代码已完成，但企业短信资质、签名/模板审核、供应商参数和 Supabase Phone Provider 仍待配置。
- ⚠️ 官方灾害预警代码已完成，线上仍需配置和风天气专属 API Host 与 API Key。
- ⚠️ 仍需用一个真实注册账号走完登录 → 找回密码 → 生成 → 收藏 → 账本 → 打卡用户态冒烟。
- ⚠️ 扩大内测前还需把实例内基础限流升级为持久化限流、增加模型自动重试并轮换 DeepSeek Key。

## 下一目标：Phase 1 内测闭环

先用真实账号跑通登录 → 找回密码 → 生成 → 收藏 → 账本 → 打卡的完整线上冒烟测试；随后让丸丸做到**会问、会记、能安全上线**：2–4 轮必要追问、稳定偏好复用、模型自动修复/重试，并邀请 5–10 位真实用户验证“可用 / 想转发 / 回头”。在此之前不启动记忆地图、论坛或语音开发。

> 以下为 2026-06-13 的完整需求与架构快照，用于保留决策历史。若其中的功能范围、存储方式或 API 路径与上方进度冲突，以 `docs/` 下的当前文档和实际代码为准。


---

# PRD v0.1 — AI 旅行管家（产品代号：待定）

> 状态：草稿 · 作者：谷盼盼（lord） + 赛巴斯蒂安
> 创建：2026-06-13
> 阶段：第 0 级「网页原型（H5）」，目标是**验证「有没有人爱用、愿不愿转发」**，不收费。

---

## 1. 背景与一句话定位

旅游规划的痛不在「查不到信息」，而在信息散、路线难排、选择瘫痪、临场易变。
大平台（携程/马蜂窝/小红书）做的是**冷冰冰的信息聚合**，给你一堆素材，最后还是你自己累死累活地排。

**一句话定位：**
> 一个**陪你走完每一程、还记得你走过哪儿**的 AI 旅行管家——**行前帮你排，行中陪你走，行后替你记**。给你的不是行程清单，是一个带审美和态度、有声音、懂你的旅伴。

差异化 = 「态度 + 懂你 + 全程陪伴 + 记住你」，这是大平台和通用 AI 都给不了的——通用 AI 只是行前问一下就走，我们陪完整程并沉淀成记忆。完整的差异化与护城河论证见 [08-travel-lifecycle-and-moat.md](08-travel-lifecycle-and-moat.md)，这是本产品的 IP 内核。

---

## 2. 目标用户 & 核心场景

- **用户**：会自由行、但懒得做攻略、有点审美要求的年轻人（第一个用户 = lord 自己）。
- **核心场景**：
  > 我下个月想去 ×× 玩 N 天，预算大概 ×× ，我不爱早起、讨厌排队、爱吃。
  > → 5 秒后拿到一份排好路线、带管家口吻点评的每日行程，能直接照着走。

---

## 3. MVP 范围（关键：做什么 / 不做什么）

### ✅ 做（v0.1 网页原型）
1. 一个输入表单：目的地、天数、预算、出发日期(选填)、旅行脾气(标签+自由文本)。
2. 生成一份**带性格的每日行程**：按 Day 1…N，每天分上午/下午/晚上，含景点、餐饮、交通提示 + 管家口吻的点评。
3. 结果可**一键复制 / 保存到本地**（localStorage），下次能看到历史。
4. 界面好看（这是「艺术品」雏形，体现 lord 的前端审美）。

### ❌ 暂不做（防止范围爆炸）
- 不做账号/登录系统（先用 localStorage）。
- 不做实时机票/酒店/天气接口（先给建议，不接真实 API）。
- 不做支付（第 0 级免费验证）。
- 不做小程序/App（验证 OK 再说）。
- 不做地图实时导航（先给文字路线）。

---

## 4. 功能详述

### 4.1 输入
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 目的地 | 文本 | 是 | 城市/国家，如「成都」「日本京都」 |
| 天数 | 数字 | 是 | 1–15 天 |
| 预算 | 选项/数字 | 否 | 穷游 / 适中 / 舒适，或具体金额 |
| 出发日期 | 日期 | 否 | 影响季节/天气建议 |
| 旅行脾气 | 多选标签 + 自由文本 | 否 | 懒觉党/特种兵、爱热闹/爱清静、爱吃、怕排队… |

### 4.2 输出（核心体验）
- 顶部：一句管家式开场白（带性格，例：「京都这季节正好，我给您排得松快些，早上不催您起床。」）
- 主体：Day 1 … Day N，每天 = 上午 / 下午 / 晚上 三段
  - 每段：地点名 + 一句为什么推荐 + 交通/时间提示
  - 关键处带「管家提醒」（如：「这家店 12 点后排长队，咱们 11 点到。」）
- 底部：一键复制全文 / 保存本程

### 4.3 历史
- 本地保存生成过的行程，列表可点开重看。

---

## 5. 信息架构 / 页面

- **P1 首页/输入页**：品牌名 + 一句 slogan + 输入表单 + 「生成行程」按钮。
- **P2 结果页**：行程展示 + 复制/保存。
- **P3 历史页**：本地保存的行程列表。

（v0.1 可以单页内切换状态，不必做路由。）

---

## 6. 技术方案（v0.1）

- **前端**：React + Vite + TypeScript（或纯 HTML 起手，看 lord 偏好）。
- **样式**：TailwindCSS，先把界面做精致。
- **行程生成**：前端 → **serverless 代理函数（藏 API Key）** → DeepSeek API（OpenAI 兼容），返回结构化行程 JSON，前端渲染。
  - ⚠️ API Key 绝不入前端（F12 即可被盗刷）。必须经后端代理。详见 `04-architecture-and-data-model.md`。
  - 原型阶段：可先用 mock 数据跑通界面，再接真 API，降低风险。
- **后端**：极轻的 serverless 函数（Vercel Functions / Cloudflare Workers），仅做 API 代理，无数据库。
- **存储**：用户行程暂存浏览器 localStorage（v0.1 无用户数据库）。
- **部署**：Vercel / Cloudflare Pages（免费、推链接即用，serverless 函数同平台托管）。

---

## 7. 数据与隐私

- v0.1 无账号、无服务器存储，数据只在用户浏览器本地。
- 接 DeepSeek API 时，用户输入会发给模型——原型阶段在页面注明即可。

---

## 8. 成功指标（怎么算「验证通过」）

第 0 级只看三个信号，**不看收入**：
1. **可用**：lord 自己用它规划了至少 1 次真实旅行，且觉得「比自己排省事」。
2. **想转发**：给 ≥5 个朋友用，有人主动说「这个不错」或转给别人。
3. **回头**：有人用了不止一次。

三个里中 ≥2 个 → 进入第 1 级（上小程序、注册个体户、接支付）。

---

## 9. 里程碑 / 路线图

| 阶段 | 产出 | 目标 |
|------|------|------|
| **M0（本周）** | PRD 定稿 + 界面原型（静态） | 把样子和体验定下来 |
| **M1** | 接 DeepSeek API，能真生成行程 | 自己能用 |
| **M2** | 部署上线，发链接给朋友 | 收集反馈 |
| **M3** | 迭代体验 → 验证三指标 | 决定是否上小程序 |

> 以上是 **Phase 1（行前 ①）** 的里程碑。验证通过后顺飞轮往下：**Phase 2** 行后（记忆地图 ③ + 游记）+ 行中轻量；**Phase 3** ② 社交 + 行中实时陪伴（地图/语音/lord 音色，需小程序/App）。全周期与分期见 [07](07-product-modules-and-phasing.md) / [08](08-travel-lifecycle-and-moat.md)。

---

## 10. 风险 & 对策

| 风险 | 对策 |
|------|------|
| 行程质量不稳定（AI 瞎编景点） | prompt 里强调真实性；后期接真实 POI 数据 |
| 和大平台同质化 | 死磕「性格 + 懂你」，不拼信息全 |
| 范围爆炸做不完 | 严守第 3 节「不做」清单 |
| 个人主体小程序不能收款 | 验证 OK 后注册个体工商户再上小程序 |

---

## 11. 已拍板决策

1. ✅ **管家人设口吻**：**温柔贴心旅伴**——像一个懂你的朋友，语气温暖、体贴，会照顾你的情绪和习惯（不爱早起就不催你起床）。这是产品的灵魂基调。
2. ✅ **技术栈**：**React + Vite + TypeScript + TailwindCSS**，部署 Vercel/Cloudflare Pages。

### 仍待确认（不阻塞动手）
- **产品/品牌名**：待定（可先用代号开工，上线前定）。
- **第一个测试用例**：lord 最近真想去的目的地 + 天数 + 旅行脾气（用真实需求当第一份行程数据）。


---

# 02 · 竞品分析

> 状态：草稿 · 2026-06-13
> ⚠️ 数据来源说明：下表为基于通用认知的定性分析，**非实时抓取**。在投入重金/重大决策前，应用 deep-research 拉一次带时间戳的最新竞品数据再复核（延续 lord 的「强制溯源」原则）。

---

## 1. 为什么要做这份分析

旅游规划是红海。不先看清对手，我们会一头撞进它们的强项里送死。
这份分析的唯一目的：**找到大平台不愿做、也做不出的那条缝**，把我们的全部力量压上去。

---

## 2. 竞品全景（按类型分）

| 类型 | 代表 | 它强在哪 | 它的死穴（=我们的机会） |
|------|------|----------|------------------------|
| OTA 交易平台 | 携程、飞猪 | 机票酒店全、能交易、资源深 | 工具是为「卖货」服务的，规划体验冷、繁、广告多；不懂你 |
| 内容种草社区 | 小红书、马蜂窝 | 真实游记多、灵感足 | 给你一堆碎片，**最后还是你自己累死累活地排**；信息过载、决策瘫痪 |
| 传统攻略/工具 | 穷游、各类行程 App | 模板、清单、地图 | 死板模板，千人一面，没温度 |
| AI 行程类(海外) | Mindtrip、Wonderplan 等 | 已经在用 AI 生成行程 | 多为英文/海外景点、偏工具理性、**没有「人格」和情感连接**；中文本地化弱 |

---

## 3. 它们共同的三个死穴

1. **冷**：都是「信息聚合器」，给素材不给陪伴。没有一个像「懂你的朋友」。
2. **累**：种草越多越焦虑，最后规划负担还在用户身上。
3. **泛**：千人一面，不在乎「你」是谁、你怕排队还是爱睡懒觉。

---

## 4. 我们的差异化定位（那条缝）

> **不拼「信息全」，拼「态度 + 懂你」。**
> 一个温柔贴心、记得你脾气的旅行管家——给的不是清单，是带情感的陪伴式规划。

- 大平台**不愿做**：人格化、情感化不利于它们的流量/交易效率，且难规模化。
- 大平台**做不出**：人格和温度是创始人审美的延伸，是 IP，不是功能堆砌能复制的。
- 这正好长在 lord 的 IP 内核上（「记忆 / 关系 / 一个懂你的存在」）。

---

## 5. 不与对手正面竞争的边界

- 不做机票酒店交易（打不过携程，也不必）。
- 不做海量 UGC 社区（打不过小红书）。
- 只做一件事并做到有灵魂：**把「想去玩」到「拿到一份贴心行程」这一步，做得又快又暖。**

---

## 6. 风险

- AI 行程生成正在变成标配，单纯「能生成」没壁垒 → **壁垒必须建在「人格 / 品牌 / 懂你（记忆）」上**，这也是后续要持续投入的方向。
- 行程真实性（AI 可能编造景点）是所有 AI 行程产品的通病 → 见架构文档的数据真实性策略。


---

# 03 · 用户画像 · 用户故事 · 用户流程

> 状态：草稿 · 2026-06-13

---

## 1. 用户画像（Persona）

### 主画像：小悠（懒得做攻略的自由行青年）
- 25–35 岁，城市白领，一年出游 2–4 次。
- **痛点**：想自由行不想跟团，但做攻略太累——查景点、排路线、看哪个不绕路，光想想就放弃，最后要么随便玩要么直接跟团。
- **心理**：怕踩坑（排队、雷店）、有点审美、希望被照顾、决策容易瘫痪。
- **一句话**：「我只想有人懂我地把行程排好，我照着走就行。」

### 次画像：第一个真实用户 = lord 谷盼盼本人
- 用自己的真实出行需求作为第一个测试用例，保证产品不脱离真实。

---

## 2. 用户故事（User Stories）

格式：作为<谁>，我想<做什么>，以便<获得什么价值>。

**核心故事（MVP 必做）**
- US-1：作为一个懒得做攻略的人，我想**输入目的地+天数+我的脾气**，以便不用自己查就能拿到行程。
- US-2：作为怕踩坑的人，我想行程里**带贴心提醒**（哪家店几点去不排队），以便少走弯路。
- US-3：作为有审美的人，我想看到**好看、温暖**的界面，以便用着舒服、愿意分享。
- US-4：作为会反复用的人，我想**保存历史行程**，以便下次还能看。
- US-5：作为想安利朋友的人，我想**一键复制/分享行程**，以便发给同行的人。

**暂不做（记录，防遗忘）**
- US-X1：登录同步多设备（v0.1 不做，用 localStorage）。
- US-X2：实时机票酒店天气（先给建议，不接 API）。
- US-X3：付费会员（第 0 级免费）。

---

## 3. 用户旅程地图（情绪曲线 → 我们插针的位置）

| 阶段 | 用户在做什么 | 情绪 | 现有方案的问题 | 我们的机会 |
|------|--------------|------|----------------|------------|
| 萌生 | 「好想出去玩」 | 兴奋 ↑ | — | 接住这股兴奋 |
| 做攻略 | 查景点/看游记/排路线 | 焦虑/疲惫 ↓↓ | **最痛**：累、乱、瘫痪 | **核心切入点**：一键替他排好 |
| 决策 | 定不下来 | 烦躁 ↓ | 选择过载 | 给「带态度的建议」帮他下决定 |
| 出行 | 照行程走 | 期待 ↑ | 临场变化无人管 | 贴心提醒（先做静态，后期可动态） |
| 回来 | 想再用/安利 | 满足 | 没复用入口 | 历史保存 + 一键分享 |

**结论**：我们把全部火力压在「做攻略」这个最痛的谷底。

---

## 4. 用户流程图（User Flow · v0.1）

```
┌─────────────┐
│  打开网页    │
│ (首页/输入页) │
└──────┬──────┘
       │ 填写：目的地 / 天数 / 预算 / 脾气
       ▼
┌─────────────┐      点「生成行程」
│  点击生成    │ ───────────────┐
└─────────────┘                │
       ▼                        ▼
┌─────────────┐         ┌──────────────┐
│  加载中…     │         │ serverless代理│→ DeepSeek API
│ (温柔文案)   │         │ (藏Key, 生成) │←  返回行程JSON
└──────┬──────┘         └──────────────┘
       ▼
┌─────────────────────────────┐
│  结果页：带性格的每日行程      │
│  · 管家开场白                 │
│  · Day1..N (上午/下午/晚上)   │
│  · 贴心提醒                   │
│  [一键复制] [保存到本地]      │
└──────┬───────────────┬──────┘
       │保存            │返回
       ▼                ▼
┌─────────────┐   ┌─────────────┐
│  历史列表    │   │  回首页重排  │
│ (localStorage)│   └─────────────┘
└─────────────┘
```

**异常流**：
- AI 生成失败/超时 → 温柔的报错文案 + 「再试一次」按钮（不暴露技术错误）。
- 输入不全（缺目的地/天数）→ 友好提示，不让用户卡住。

---

## 5. 这份文档怎么用

- 开发时，每个功能对照「用户故事」编号，确保不做无关的事。
- 设计原型时，严格按「用户流程图」画页面，不拍脑袋加页面。


---

# 04 · 技术架构 + 行程数据模型

> 状态：草稿 · 2026-06-13 · 作者：赛巴斯蒂安 + lord
> 本文是 Phase 1（① 行程规划）的工程地基：技术架构图、行程 JSON 数据模型、数据真实性策略、Key 安全。
> 被 [PRD-v0.1.md](PRD-v0.1.md)（§6 技术方案）与 [02-competitive-analysis.md](02-competitive-analysis.md)（§6 真实性）引用。

---

## 1. 技术架构图

核心约束：**LLM API Key 绝不能进前端**（F12 即可被盗刷）。所以前端永远不直连 DeepSeek，必须经一层 serverless 代理函数中转。

```
┌──────────────────────────────────────────────────────────────┐
│                         浏览器（用户）                          │
│  React + Vite + TS + Tailwind  —— 静态站点，部署在 CDN          │
│                                                                │
│  ┌────────────┐   ┌─────────────┐   ┌──────────────────────┐  │
│  │ 输入/对话页 │ → │  加载态     │ → │ 结果页（渲染行程JSON） │  │
│  │ (提问引导)  │   │ (温柔文案)  │   │  + 一键复制/保存       │  │
│  └────────────┘   └─────────────┘   └──────────┬───────────┘  │
│         │                                       │ 保存          │
│         │ POST /api/plan                        ▼              │
│         │ { destination, days, budget,   ┌─────────────┐       │
│         │   tags, note }                 │ localStorage │       │
│         │                                │ (历史行程)   │       │
│         ▼                                └─────────────┘       │
└─────────┼──────────────────────────────────────────────────────┘
          │  HTTPS（同源，无 Key）
          ▼
┌──────────────────────────────────────────────────────────────┐
│      Serverless 代理函数（Vercel Functions / CF Workers）      │
│                                                                │
│  · 唯一持有 DEEPSEEK_API_KEY（环境变量，服务端）              │
│  · 校验/限流/拼装 system+user prompt                          │
│  · 调 DeepSeek，response_format=json_object 强制 JSON          │
│  · 把模型返回的行程 JSON 透传给前端                            │
│  · 错误兜底 → 返回温柔文案给前端（不暴露技术错误/不泄 Key）    │
└─────────┼──────────────────────────────────────────────────────┘
          │  HTTPS + Authorization: Bearer（仅服务端可见）
          ▼
┌──────────────────────────────────────────────────────────────┐
│              DeepSeek API（OpenAI 兼容 /chat/completions）      │
│         model: deepseek-chat · 返回结构化行程 JSON             │
└──────────────────────────────────────────────────────────────┘
```

### 1.1 为什么是这套
| 决策 | 选择 | 理由 |
|------|------|------|
| 前端框架 | React + Vite + TS + Tailwind | PRD §11 已拍板；Vite 起手快，TS 给 JSON 模型加类型护栏 |
| 后端 | 极轻 serverless（无数据库） | MVP 只需「藏 Key + 代理」，不引入运维负担 |
| 部署 | Vercel / Cloudflare Pages | 前端静态 + serverless 函数同平台托管，免费、推链接即用 |
| 存储 | 浏览器 localStorage | v0.1 无账号、无服务端数据库（PRD §3「不做」清单） |

### 1.2 LLM 供应商：DeepSeek（永久定，2026-06-13 lord 拍板）

**全程用 DeepSeek，不切换其他供应商。** 接口是 OpenAI 兼容的 `/chat/completions`。

- 代理函数里仍抽一个薄客户端 `llm.ts`（封装 endpoint/key/JSON 解析），把对 DeepSeek 的调用收口到一处，方便统一处理重试、校验、错误兜底——这是工程卫生，不是为了换供应商。
- Key 只在服务端代理读（`DEEPSEEK_API_KEY`），前端永不接触。
- ⚠️ 这把 key 曾在聊天明文出现，**正式上线前到 DeepSeek 后台重置一次**。

### 1.3 DeepSeek 调用参数
| 项 | 取值 | 说明 |
|----|------|------|
| 接口 | `POST https://api.deepseek.com/chat/completions` | OpenAI 兼容；鉴权 `Authorization: Bearer $DEEPSEEK_API_KEY` |
| 主模型 | `deepseek-chat` | 通用对话/生成，性价比高，行程生成够用 |
| 推理备选 | `deepseek-reasoner` | 复杂多日跨城路线若要更强推理可切；自带思考、更慢更贵 |
| 强制 JSON | `response_format: {type: "json_object"}` | **必须在 prompt 里显式描述 `Itinerary` 字段结构**——DeepSeek 不按 schema 校验 |
| 温度 | `temperature: 1.0` 左右 | DeepSeek 支持温度调节；生成行程偏创意，1.0 附近即可，过低会刻板 |
| max_tokens | 设足够大（如 8192） | 多日行程 JSON 较长，给够余量免截断 |
| 流式 | 可选 | 结果页若要逐字呈现可开 `stream:true`；否则一次性返回更简单 |

> 注意：DeepSeek 走 OpenAI 兼容那套参数，**没有**模型专属的 schema 强制/思考预算等参数。强制 JSON 靠 `json_object` 模式 + prompt 描述 + 代理侧二次校验三件套。

### 1.4 上下文缓存（DeepSeek 自带，省钱）
DeepSeek 对**重复的前缀**自动做上下文缓存（cache hit 部分计费大幅降低），无需手动声明。
- 想吃到这个红利：把**不变的内容（管家 system prompt）放在每次请求的最前面、保持逐字一致**；动态内容（用户的目的地/脾气）放到后面的 user 消息。
- 铁律：system prompt 里**绝不能插入**当前时间/随机 ID/用户名等每次都变的内容，否则前缀不一致、缓存命中不了。

---

## 2. 接口契约（前端 ↔ 代理）

### 2.1 请求 `POST /api/plan`
```jsonc
{
  "destination": "京都",          // 必填
  "days": 4,                      // 必填 1–15
  "departureDate": "2026-07-10",  // 选填，"" 表示未提供
  "pace": "leisurely",            // 选填: packed | balanced | leisurely（节奏）
  "companions": "couple",         // 选填: solo | couple | friends | family | other
  "mustVisit": ["清水寺"],         // 选填，必去清单，可为 []
  "avoid": "不爱人挤人的网红点",     // 选填，避雷，"" 表示无
  "budgetTier": "moderate",       // 选填: budget | moderate | comfort | custom
  "budgetNote": "大概一万以内",    // 选填，自由文本
  "travelerTags": ["懒觉党", "爱吃", "怕排队"],  // 选填，软性标签（作息/兴趣/交通…）
  "travelerNote": "住祇园附近"     // 选填，自由文本兜底
}
```

### 2.2 响应
- 成功：`200` + 行程 JSON（§3 的 `Itinerary`）。
- 失败：`200` + `{ "ok": false, "friendlyMessage": "管家口吻的温柔报错" }`（前端只展示 friendlyMessage，绝不暴露堆栈/Key）。

> 异常流对齐 [03-user-stories-and-flow.md](03-user-stories-and-flow.md) §4：AI 失败/超时 → 温柔文案 + 「再试一次」；输入不全 → 友好提示。

---

## 3. 行程 JSON 数据模型

这是**前端渲染、模型输出、本地存储三者共用的契约**。下面给 TypeScript 类型（前端用）+ JSON 结构约束要点（写进 prompt 让 DeepSeek 照着产出）+ 一个真实样例。

### 3.1 TypeScript 类型
```ts
// 与结果页渲染、localStorage 历史一一对应
export interface Itinerary {
  meta: ItineraryMeta;
  greeting: string;        // 管家开场白（带性格），对应 PRD §4.2 顶部
  days: DayPlan[];         // 长度 = meta.days
  closing: string;         // 结语
  disclaimer: string;      // 真实性免责提示（固定提示，见 §4）
}

export interface ItineraryMeta {
  destination: string;
  days: number;            // 1–15
  departureDate: string;   // ISO 日期 或 ""
  pace: "packed" | "balanced" | "leisurely";  // 节奏：紧凑/适中/溜达
  companions: "solo" | "couple" | "friends" | "family" | "other";  // 同行人
  mustVisit: string[];     // 必去清单（强约束），可为 []
  avoid: string;           // 避雷（负约束），"" 表示无
  budgetTier: "budget" | "moderate" | "comfort" | "custom";
  budgetNote: string;      // "" 表示未提供
  travelerTags: string[];  // 软性标签：作息/兴趣/怕排队/交通偏好…（见 06 提问库）
  travelerNote: string;    // 自由文本兜底（住处位置等）
  season: string;          // 管家据出发日期推断的季节提示，可为 ""
}

export interface DayPlan {
  dayIndex: number;        // 从 1 开始
  date: string;            // "" 表示未排具体日期
  theme: string;           // 当天主题，如「古都漫步」
  segments: Segment[];     // 固定 3 段：上午/下午/晚上
}

export interface Segment {
  period: "morning" | "afternoon" | "evening";
  items: Item[];           // 该时段的若干安排
}

export interface Item {
  type: "sight" | "food" | "transport" | "rest" | "activity";
  name: string;            // 地点/店名/活动名
  area: string;            // 所在区域，如「东山区」
  why: string;             // 一句为什么推荐（管家视角）
  butlerTip: string;       // "" 或贴心提醒，对应 PRD「管家提醒」
  timeHint: string;        // "" 或时间提示，如「11:00 前到」
  durationHint: string;    // "" 或「约 2 小时」
  costHint: string;        // "" 或「人均 80 元」
  imageQuery: string;      // 取真实配图用的检索词，如「京都 清水寺」；图片由前端/后端按它取，模型不编 URL（防编造）
  confidence: "high" | "medium" | "low";  // 模型对该条真实性的自评，见 §4
}
```

### 3.2 让 DeepSeek 稳定产出此结构的要点
DeepSeek 的 `json_object` 模式只保证「输出是合法 JSON」，**不按 schema 校验字段**。所以结构靠三件套保证：

1. **prompt 里把上面的字段结构、枚举值、每天三段写清楚**（见 [05 §4.1](05-butler-prompt-design.md)），并给一个示例输出。
2. **约定写死**：选填字段用空串 `""` / 空数组 `[]`，不用 `null`；枚举值固定（`budgetTier`/`period`/`type`/`confidence`），简化前端渲染。
3. **代理侧二次校验**（必做）：用 TS 类型 / zod 校验 days 段数、dayIndex 连续、period 三段齐、枚举合法；不合法重试一次或降级温柔提示。

> 校验 schema 落到 `src/schema/itinerary.ts`（TS 类型 + 运行时校验同源，单一事实来源），前端渲染、代理校验都引它。

### 3.3 样例（节选，2 天）
```jsonc
{
  "meta": {
    "destination": "京都", "days": 2, "departureDate": "2026-07-10",
    "pace": "leisurely", "companions": "couple",
    "mustVisit": ["清水寺"], "avoid": "不爱人挤人的网红点",
    "budgetTier": "moderate", "budgetNote": "大概一万以内",
    "travelerTags": ["懒觉党", "爱吃", "怕排队"],
    "travelerNote": "住祇园附近", "season": "盛夏，午后闷热多雷阵雨"
  },
  "greeting": "京都这季节正好，我给您排得松快些，早上不催您起床。午后最热，咱们躲进店里慢慢吃。",
  "days": [
    {
      "dayIndex": 1, "date": "2026-07-10", "theme": "东山慢走",
      "segments": [
        { "period": "morning", "items": [
          { "type": "rest", "name": "睡到自然醒，酒店早餐", "area": "市中心",
            "why": "您不爱早起，第一天不赶", "butlerTip": "", "timeHint": "10:00 前出门即可",
            "durationHint": "", "costHint": "", "confidence": "high" }
        ]},
        { "period": "afternoon", "items": [
          { "type": "sight", "name": "清水寺", "area": "东山区",
            "why": "京都门面，坡道老街顺路逛", "butlerTip": "怕排队的话别赶黄昏，午后人相对少",
            "timeHint": "", "durationHint": "约 2 小时", "costHint": "门票 400 日元",
            "confidence": "high" }
        ]},
        { "period": "evening", "items": [
          { "type": "food", "name": "祇园一带的怀石小店", "area": "祇园",
            "why": "您爱吃，晚上避开正午暑气慢慢吃", "butlerTip": "热门店建议提前订位",
            "timeHint": "", "durationHint": "", "costHint": "人均约 8000 日元",
            "confidence": "medium" }
        ]}
      ]
    }
  ],
  "closing": "两天排得不紧，剩下的力气留给您随心逛。要我换口味或加一天，随时说。",
  "disclaimer": "行程由 AI 生成，景点营业时间/价格请出行前再核实一次。"
  // 注：为简洁，上面 item 省略了 imageQuery；实际每条都应带，值如「京都 清水寺」，供取真实配图
}
```

---

## 4. 数据真实性策略（对齐竞品分析 §6 风险）

AI 行程的通病是**编造景点/瞎报价格**。对策分三层：

1. **prompt 层**：在 system prompt 明确「只推真实、知名度高、可被查证的地点；不确定就说不确定，不要编造精确地址/电话/具体价格」。详见 [05-butler-prompt-design.md](05-butler-prompt-design.md) 的防编造段。
2. **模型自评层**：每个 `Item` 带 `confidence`（high/medium/low）。前端对 `low` 可加视觉弱化或「请再核实」小标。
3. **固定免责**：`disclaimer` 字段固定提示「出行前再核实营业时间/价格」。

> 这与 lord 的「强制溯源」原则同源：MVP 阶段无法逐条挂数据源，就用 `confidence` + 免责把不确定性显式暴露给用户，而不是假装都对。后期接真实 POI 数据时，可把 `confidence` 升级为带来源的事实。

---

## 5. 落地顺序（M0 → M1，对齐 PRD 路线图）
1. 先用 **mock 行程 JSON**（就用 §3.3 样例扩充）跑通前端三页 + 渲染 + 复制/保存。**界面先做精致**。
2. 再写 `/api/plan` 代理函数，接 DeepSeek API，本地 `.env.local` 放 Key。
3. 用 lord 真实出行需求当第一份请求验证（PRD §11 待确认项）。
4. 部署 Vercel，发链接。

---

## 6. 导出与配图（Phase 1 差异化，对齐 [08](08-travel-lifecycle-and-moat.md)）

**导出（Phase 1 就做）**——把结果页变成一张能晒的成品，也是增长钩子：
- **图片**：`html-to-image` / `html2canvas` 把结果页 DOM 渲成 PNG，做一张**竖版分享卡**（适合发朋友圈/小红书 → 反向引流）。
- **PDF**：`jsPDF` / `html2pdf.js`，或 PNG 塞进 PDF。
- 纯前端库，CDN 可加载，不碰后端，MVP 可行。

**配图来源**（重要：不编、不爬）：
- ❌ 不用 AI 生成图（会画出不存在的景）；❌ 不爬小红书（无开放 API + 合规 + 脆弱）。
- ✅ MVP 先用**风格化卡片**（暖色块 + icon + 排版），导出已够好看。
- ✅ 接后端后，按 `Item.imageQuery` 从**地图 POI 图库 / 正版图库**取真实照片（可商用授权）。
- 反向利用：把分享卡做得适合发小红书，借它流量引流，而不是爬它数据。


---

# 05 · 管家 Prompt 设计

> 状态：草稿 · 2026-06-13 · 作者：赛巴斯蒂安 + lord
> 本文定义产品灵魂——管家的人设、提问引导式对话流、行程生成 prompt、防编造规则。
> 依赖 [04-architecture-and-data-model.md](04-architecture-and-data-model.md)（数据模型 + API 参数）。

---

## 0. 设计目标

管家 = 产品 IP 内核。三件事必须同时成立：
1. **有性格、懂你**：温柔贴心旅伴口吻（PRD §11 拍板的灵魂基调），照顾用户情绪和习惯。
2. **输出能渲染**：最终必须吐出符合 `Itinerary` schema 的 JSON，前端直接渲染。
3. **不编造**：宁可说「不确定」，不许瞎编景点和价格。

技术现实（供应商 = DeepSeek，永久）：DeepSeek 的 `json_object` 模式只保证合法 JSON、**不校验字段结构**，所以「人设+规则+目标结构描述」全压在 **system/user prompt** 里讲清楚，再靠代理侧二次校验兜底。

---

## 1. 两段式调用结构

提问引导式体验拆成两类调用，复用同一个管家 system prompt（同前缀，利于缓存）：

| 调用 | 何时 | system | 输出格式 | 目的 |
|------|------|--------|----------|------|
| **A 对话收集** | 用户还没填齐信息，管家逐步追问 | 管家人设 + 收集规则 | 自由文本（管家的下一句话）+ 一个轻量「槽位状态」JSON | 像聊天一样把目的地/天数/预算/脾气问出来 |
| **B 行程生成** | 槽位齐了（至少目的地 + 天数） | 管家人设 + 生成规则 + 防编造 | `json_object` + prompt 描述 Itinerary 结构 | 产出最终行程 JSON |

> MVP 可先只做 **B**（配一个极简表单兜底输入），把 **A 的提问引导**作为紧接着的体验升级——但 system 人设两者共用，先写好不浪费。

---

## 2. 管家人设（system prompt 公共头）

```text
你是「赛巴斯蒂安」式的旅行管家——一个温柔、贴心、记得用户脾气的旅伴，
不是冷冰冰的信息工具。你的语气温暖、体贴，会照顾用户的情绪和生活习惯：
他不爱早起，你就不把行程排在大清早；他怕排队，你就提醒错峰；他爱吃，
你就在吃上多花心思。

你给的不是一份景点清单，是一段被妥帖安排好的旅程。说话像懂他的朋友，
有态度、有审美，但不啰嗦、不堆砌、不卖弄。中文回复。

你绝不编造：只推荐真实、知名、可被查证的地点。任何不确定的信息
（精确地址、电话、当下票价、营业时间）宁可不写或注明「请出行前再核实」，
也不许编造看起来很像真的细节。
```

> 风格靠 prompt + 适中 temperature（DeepSeek 支持温度，1.0 附近偏自然，过低会刻板）。若觉得太啰嗦/太冷，改这段 + 加正向示例，比加「不要 XX」更有效。

---

## 3. A · 提问引导式对话 prompt

### 3.1 要收集的槽位
**完整问题清单与措辞见 [06-butler-questions.md](06-butler-questions.md)**，这里只列槽位与优先级：

| 槽位 | 档位 | 说明 |
|------|------|------|
| destination | 必问 | 目的地 |
| days | 必问 | 天数 1–15 |
| departureDate | 必问* | 季节/天气；不确定可跳 |
| pace | 体验核心 | 紧凑/适中/溜达 |
| companions | 体验核心 | 同行人，影响强度与节奏 |
| travelerTags | 体验核心 | 作息/兴趣/怕排队/交通等软标签 |
| mustVisit | 锦上添花 | 必去清单（强约束） |
| avoid | 锦上添花 | 避雷（负约束） |
| budgetTier / budgetNote | 锦上添花 | 预算档或描述 |
| travelerNote | 锦上添花 | 住处位置等自由兜底 |

**生成触发条件**：`destination` 与 `days` 齐 → 可转 B 生成。其余缺失不阻塞，管家一句话带过（「预算和喜好你没说，我先按溜达松快来，不满意再改」）。**别把全集都问完**——核心齐了就主动提议先排一版（纪律见 [06 §0](06-butler-questions.md)）。

### 3.2 收集规则（接在 §2 人设后）
```text
现在你在和用户聊天，目的是温柔地把这次旅行的信息问清楚。
必问：目的地、天数、出发日期。再尽量了解：节奏（紧凑/适中/溜达）、
同行人、作息与喜好、有没有必去的地方、有没有想避开的、预算。

规则：
- 一次只问一两个问题，别像填表一样连珠炮。语气自然，像朋友聊天。
- 按优先级问：必问 → 节奏/同行/喜好 → 必去/避雷/预算。别一口气全问完。
- 用户已经说过的别再追问。能从上下文推断的（如「下个月去玩四五天」）就别死抠。
- 必问项凑齐后，不要无限追问——主动提议「我先给你排一版，边看边调」。
- 每轮回复末尾，附机器可读的槽位状态，用 <slots>...</slots> 包裹，里面是 JSON：
  { destination, days, departureDate, pace, companions, mustVisit,
    avoid, budgetTier, budgetNote, travelerTags, travelerNote, ready }。
  未知字段留空串/空数组，ready 为「目的地+天数」是否齐。
```

> 代理函数解析 `<slots>` 后面的 JSON 决定下一步：`ready=true` 就拿槽位去调 B。把状态裹在标签里而不是用结构化输出，是因为 A 同时要输出**自然语言对话**——两者混在一条消息里，用标签切开最简单。

---

## 4. B · 行程生成 prompt

### 4.1 生成规则（接在 §2 人设后）
```text
根据用户提供的信息，生成一份带性格的每日行程。要求：

结构：
- 按天编排，每天分上午/下午/晚上三段；总天数 = 用户给的天数。
- 开场白(greeting)用管家口吻，点出你怎么照顾他的脾气（如不催早起、避开人挤）。
- 结语(closing)收尾，邀请他让你调整。

内容：
- 每个安排写清：名称、所在区域、一句为什么推荐（你的视角）。
- 关键处给「管家提醒」(butlerTip)：哪家店几点去不排队、午后躲热、提前订位之类。
- 贴合他的脾气：懒觉党别排早场，怕排队的提示错峰，爱吃的多给吃。
- **按节奏(pace)定密度**：紧凑→每天多排几处；溜达→留白多、节奏慢。
- **必去清单(mustVisit)必须排进去**；避雷(avoid)里的一律回避。
- **照顾同行人(companions)**：带爸妈/带娃就降强度、少走路、多歇脚。
- 时间/时长/花费提示能给就给，给不准就留空，别硬编。

真实性（重要）：
- 只用真实、知名、可查证的地点。不确定的地址/电话/票价/营业时间不要编，
  留空或写大致范围。
- 每条给 confidence：high=很有把握的知名地点；medium=方向对但细节请核实；
  low=不太确定。绝不为了显得专业而编造精确数字。
- disclaimer 固定提示出行前核实营业时间与价格。
```

### 4.2 强制 JSON（代理函数侧，DeepSeek）
- 调用传 `response_format:{type:"json_object"}`，并在 prompt（§4.1 末尾）**显式列出 `Itinerary` 的字段结构、枚举值，附一个示例输出**——DeepSeek 不按 schema 校验，全靠描述。
- `temperature` 1.0 附近（生成偏创意，过低会刻板）；`max_tokens` 给足（多日行程 JSON 长，如 8192）。
- **二次校验（必做）**：days 段数、dayIndex 连续、period 三段齐、枚举合法；不合法就重试一次或降级温柔提示。

### 4.3 user 消息拼装
把 §3.1 收集到的槽位拼成一段自然语言塞进 user 消息（动态内容放这里，**不要污染 system 前缀的缓存**）：
```text
我想去{destination}玩{days}天。预算{budgetTier/budgetNote}。
出发日期{departureDate}。我的旅行喜好：{travelerTags + travelerNote}。
请按你管家的风格给我排一版。
```
缺失字段自然省略或写「没特别要求」。

---

## 5. 与缓存/成本的咬合
- §2 人设 + §3/§4 规则 = **稳定前缀**，放在每次请求最前面且逐字一致 → 命中 DeepSeek 自动上下文缓存，cache 部分计费大幅降低。
- 用户特定信息（槽位）永远在 user 消息，绝不进 system——否则前缀每次变、缓存命中不了。
- 把防编造细则、结构描述、few-shot 示例都放进稳定前缀：既提质量又喂饱缓存，一举两得。

---

## 6. 待验证（上线后用真实数据迭代）
- 提问引导式追问几轮最舒服？（太多轮烦，太少不懂你）
- `deepseek-chat` 够不够？复杂多日跨城行程若推理吃力，可试 `deepseek-reasoner`。
- confidence 的 low 在前端怎么呈现最不破坏体验。
- 第一份真实测试：lord 最近真想去的目的地（PRD §11 待确认）。


---

# 06 · 管家提问库（提问引导式问题清单）

> 状态：草稿 · 2026-06-13 · 作者：赛巴斯蒂安 + lord
> 本文是管家「提问引导式」收集信息时**要问哪些问题**的总清单。
> 配套：[05-butler-prompt-design.md](05-butler-prompt-design.md)（怎么问 = 对话 prompt）、[04-architecture-and-data-model.md](04-architecture-and-data-model.md)（答案存哪 = 数据模型）。

---

## 0. 设计原则（先读，别变成查户口）

问得多 ≠ 懂你。这份清单是「**可问的全集**」，不是「每次都全问」。管家的纪律（见 [05 §3.2](05-butler-prompt-design.md)）：

1. **一次只问一两个**，像朋友聊天，不连珠炮、不像填表。
2. **能推断的别问**（「下个月去玩四五天」→ 不再追问天数）。
3. **核心齐了就主动提议生成**：「我先给你排一版，边看边调」——剩下的可选项边聊边补。
4. **优先级**：必问 → 体验核心 → 锦上添花。token/耐心有限时，从下往上砍。
5. 选项用 **chip 点选**为主、自由文本兜底，降低用户打字负担。

---

## 1. 必问（缺了排不了）

| 问题（管家口吻示例） | 收集字段 | 选项 / 形式 | 怎么用 |
|----------------------|----------|-------------|--------|
| 想去哪儿玩？ | `destination` | 自由文本 | 行程主体 |
| 待几天？ | `days` | 数字 1–15 | 决定 Day 数 |
| 大概什么时候出发？（不确定也没事） | `departureDate` | 日期 / 跳过 | 推断季节、给天气与穿衣提示 |

---

## 2. 体验核心（决定「懂不懂你」，强烈建议问）

| 问题（管家口吻示例） | 收集字段 | 选项（chips） | 怎么用 |
|----------------------|----------|---------------|--------|
| 想玩得紧凑点多看几处，还是溜达一些慢慢晃？ | `pace` | 紧凑(多看) / 适中 / 溜达(躺平) | 控制每天景点密度与赶路强度 |
| 早起没问题，还是想睡到自然醒？ | `travelerTags` | 早起鸟 / 懒觉党 | 决定每天起始时间，懒觉党不排早场 |
| 这趟你最看重啥？挑几个。 | `travelerTags` | 历史人文 / 自然风光 / 美食 / 购物 / 小众宝藏 / 网红打卡 / 夜生活 | 选点的口味权重 |
| 怕排队吗？ | `travelerTags` | 怕排队 / 不在乎 | 触发错峰提醒、避开高峰时段 |
| 这趟是一个人，还是和谁一起？ | `companions` | 独自 / 情侣 / 朋友 / 带爸妈 / 带娃 | 影响强度、选点与节奏（带老人/娃更松） |

---

## 3. 锦上添花（问得到更贴心，问不到不阻塞）

| 问题（管家口吻示例） | 收集字段 | 选项 / 形式 | 怎么用 |
|----------------------|----------|-------------|--------|
| 有没有非去不可的地方？某个景点、某家店，我一定排进去。 | `mustVisit` | 自由文本（可多个） | 强约束：必须出现在行程里 |
| 有没有特别不想去的、或踩过雷的？我帮你避开。 | `avoid` | 自由文本 | 负约束：生成时回避 |
| 预算大概什么档？说个数或「适中」都行。 | `budgetTier` / `budgetNote` | 穷游 / 适中 / 舒适 / 具体金额 | 选点与餐饮档次 |
| 走路多你介意吗？还是尽量少折腾、多打车？ | `travelerTags` | 走得动 / 尽量少走 / 打车为主 | 路线紧凑度、交通建议 |
| 住的地方定了吗？定了告诉我大概位置。 | `travelerNote` | 自由文本 | 按住处就近安排每天起止点 |
| 还有什么想让我知道的？ | `travelerNote` | 自由文本 | 兜底，接住一切没覆盖的诉求 |

---

## 4. 字段去向小结（与 [04 数据模型](04-architecture-and-data-model.md) 对齐）

- **专属结构化字段**：`destination` `days` `departureDate` `pace` `mustVisit` `avoid` `companions` `budgetTier` `budgetNote`。
- **软性标签塞 `travelerTags`**（作息 / 兴趣 / 怕排队 / 交通偏好等 chip 选择）。
- **任何没覆盖的**塞 `travelerNote` 自由文本。
- 收集态在对话里用 `<slots>…</slots>` 回传（见 [05 §3.2](05-butler-prompt-design.md)），`ready` 为 `destination`+`days` 是否齐。

---

## 5. 待验证
- 体验核心 5 问全问会不会太长？可能要砍到 2–3 问 + 一句「其余我先替你拿主意」。
- chip 选项措辞与数量，上线后看真实点选率调整。
- `companions=带娃/带爸妈` 时节奏是否要自动降一档（强约束 or 仅提示）。


---

# 07 · 产品模块与分期

> 状态：草稿 · 2026-06-13 · 决策人：lord
> 本文定义产品的完整模块组成，以及为解开「社交冷启动死结」而设计的分期上线策略。

---

## 1. 三大模块（完整愿景）

| 模块 | 描述 | 本质 | 单人是否成立 |
|------|------|------|--------------|
| **① 行程规划** | 用户提供时间/地点/预算等 → **以「提问引导式」为主**（像管家聊天一步步问），AI 生成带性格的每日行程 | 工具内核 | ✅ 成立 |
| **② 论坛 / 朋友圈** | 用户上传当地各种信息（图文），带社交属性，类似朋友圈 | UGC 社交 | ❌ 需人多 |
| **③ 中国地图** | 中国地图，可缩放，查看省/市的特色；用户到过的地方浮现其**头像气泡** | 记忆可视化 + 社交 | ⚠️ 见下 |

### 已定决策
- **① 输入方式**：以**提问引导式**为主（form 手填作为后续补充，非 MVP）。理由：最贴合「温柔贴心管家」人设，对话感强。

---

## 2. 关键产品判断（为什么不能三个一起上）

### 判断一：只有 ① 单人就有价值，②③ 有「冷启动死结」
新产品最容易死在「社交功能上线即空城」——论坛没人发帖是空的，地图上没别人的气泡也是空的。若三个一起上，用户打开看到的是一座空城，自己都没动力维护。

### 判断二：地图是产品灵魂，且能「单人先跑」
「到过的地方浮现头像气泡」与 lord 一贯的执念——**记忆 / 走过的痕迹 / 被记住**——是同一个东西。
所以地图首先应做成**「你自己的旅行记忆地图」**：你去过哪，地图就点亮哪，配上当时的行程/照片。**这个版本不需要任何其他用户就成立**，单人就很动人；等有了用户，再让「别人的头像气泡」浮上来，自然进化成社交。

> 这一步把冷启动死结解开了：**地图先做单人记忆地图，社交是它后来的升级，不是前提。**

### 判断三：数据依赖关系 ① → ② → ③
地图上的「到过 / 特色 / 气泡」数据，来源于用户的行程记录与上传内容。
因此数据流与开发顺序都应是 ①（产生行程）→ ②（产生 UGC/打卡）→ ③（聚合可视化）。

---

## 3. 分期上线策略

```
Phase 1（MVP）  ① 行程规划（提问引导式管家）
                目标：单人即有用，验证「有人爱用、愿转发」

Phase 2         ③ 个人记忆地图
                你去过的城市点亮 + 城市/省份特色；仍是单人功能
                目标：情感钩子，提升留存与「我的痕迹」归属感

Phase 3         ② 论坛 / 朋友圈  +  ③ 升级为社交地图
                用户上传当地信息；地图上浮现「别人的头像气泡」
                目标：有了用户基数后再开社交，形成网络效应与护城河
```

---

## 4. 各模块技术要点（初步，待 ③a 架构文档细化）

- **① 行程规划**：提问引导式对话 → 结构化收集（目的地/天数/预算/脾气）→ 经 serverless 代理调 DeepSeek API → 返回行程 JSON → 渲染。
- **② 论坛/朋友圈**：需要真正的后端 + 数据库 + 用户账号 + 图片存储（对象存储）+ 内容审核。**比 ① 重得多**，故置于 Phase 3。
- **③ 地图**：中国地图可缩放、省/市下钻。技术候选：高德/百度地图 SDK（真实地理）或 ECharts 中国地图（风格化）。气泡 = 自定义 marker。Phase 2 单人版只需本地/轻后端记录「去过的城市」。

---

## 5. 对 MVP（Phase 1）范围的影响

- PRD v0.1 的 MVP 范围**维持不变**：先只做 ① 行程规划（提问引导式）。
- ②③ 进入产品愿景与路线图，但不进入 Phase 1 开发。
- 防范围爆炸的纪律不变：MVP 只交付 ① 一个模块并打磨到有灵魂。

> 📌 模块是「能力块」，还有一条「时间轴」视角——**行前 / 行中 / 行后**全周期，以及由此构成的记忆飞轮与护城河，见 [08 旅行全周期与护城河](08-travel-lifecycle-and-moat.md)。三大模块各自服务生命周期的不同阶段。


---

# 08 · 旅行全周期与护城河（行前 · 行中 · 行后）

> 状态：草稿 · 2026-06-13 · 作者：赛巴斯蒂安 + lord
> 回答两个根本问题：① 我们和「直接问 AI」到底有什么区别？② 为什么不能只管行前、还要管行中行后？
> 关联：[07 模块分期](07-product-modules-and-phasing.md)、[02 竞品](02-competitive-analysis.md)、[PRD](PRD-v0.1.md)。

---

## 1. 根本差异：一次性 AI 只管「行前一问」，我们陪完整程

**残酷的实话**：如果只是「聊天框 → 吐一段文字」，和直接问 DeepSeek/豆包没区别，套壳必死。差异不能建在「会生成行程」上，要建在通用 AI 给不了的东西上：

| 层 | 通用 AI | 我们 |
|----|---------|------|
| 成品化 | 一坨文字，自己整理 | 排好版、可导出图片/PDF 的成品 |
| **陪伴** | **一次性，问完即走** | **行前→行中→行后全程在场** |
| 记忆 | 对话即焚，不记得你 | 沉淀历史、越用越懂你、点亮你走过的地方 |
| 社交 | 永远单机 | 别人的真实痕迹，网络效应 |

> **核心洞察**：通用 AI 是**一个瞬间**（行前问一下）；我们是**一段关系**（陪你走完、记住你）。这正好长在 lord 的 IP 内核——「记忆 / 被记住 / 一个懂你的存在」——上。

---

## 2. 三段式旅行生命周期

### 🧭 行前 · Plan（已是 ① 行程规划核心）
- 提问引导式生成带性格的行程（[05](05-butler-prompt-design.md)/[06](06-butler-questions.md)）。
- 导出竖版**分享卡 / PDF**：揣兜里、发同行群、晒社交（兼增长钩子）。
- 行李 / 天气 / 订位 / 门票提示。

### 🚶 行中 · Travel（新增重点——通用 AI 完全缺位的战场）
| 功能 | 为什么通用 AI 做不到 |
|------|----------------------|
| **行程随身**：今天去哪、按时段卡片，导出后离线也能看 | 它不知道「你此刻在路上」 |
| **实时地图 + 周边推荐**：到了一处景点，自动搜罗附近的美食 / 好玩的，按你口味用管家口吻推 | 它不知道「你现在站在哪」 |
| **语音实时陪伴**：管家用声音陪你走，边走边能问能答（声音先用 lord 本人音色克隆） | 它没有「边走边说、声音熟悉」的在场感 |
| **临场实时调整**：下雨了/景点关门/走累了 → 管家改今天剩下的安排 | 它不知道你刚出了状况，也不记得原计划 |
| **即时贴心提醒**：基于时间/位置（11 点前到避队、天黑前回、末班车） | 它没有「现在几点、你在哪」的上下文 |
| **随手打卡**：拍照、记一句心情 | —— 这些沉淀直接喂给「行后」与记忆地图 |

> 管家从「攻略生成器」变成「身边的旅伴」——有声音、知道你在哪、能随时给附近的建议。这一段是和纯 AI 拉开差距最狠的地方。

#### 行中这层的技术与平台现实（务实，避免画饼）
- **地图 + 周边推荐**：用高德/百度地图 SDK + POI 周边搜索（可商用授权，有美食/景点/评分/营业时间）。需用户授权**定位权限**。
  - ⚠️ 此处的「地图」是**行中实时定位地图（工具属性）**，与 ③ 模块的「行后记忆地图（点亮痕迹、情感属性）」是两种用途、共用同一套地图 SDK。
- **语音**：管家说话 = TTS（文字转语音）；用户说话 = ASR（语音识别）；实时陪伴 = 双向打通。
- **声音用 lord 本人**：做音色克隆，最贴「赛巴斯蒂安」人格——管家用您的声音说话。
  - ✅ 是您本人声音、您本人授权，没有第三方肖像/声音问题。
  - ⚠️ 声音克隆模型是**敏感资产**，须妥善保管、不外泄；若将来面向公众规模化，再评估是否继续用真人声或换合成音。
- **平台现实**：实时定位 + 麦克风 + 常驻陪伴，基本是**小程序 / App 的活**，纯 H5 网页体验受限。这正好对齐 PRD「验证 OK 再上小程序」——**行中实时陪伴是上小程序后的杀手锏**，不强塞进 Phase1 网页原型。
- **分期**：以上归 **Phase 2 / 3 的行中**，Phase 1 不做（守纪律见 §4）。

### 📖 行后 · Remember（新增重点——记忆飞轮的起点）
| 功能 | 价值 |
|------|------|
| **一键生成游记/回忆卡**：行中的照片+足迹+行程 → 管家口吻的精美游记，可导出可分享 | 又一个增长钩子；把「一次旅行」变成「一份能晒的作品」 |
| **点亮记忆地图**：这趟去过的城市/省份点亮（③ 模块） | 「你的旅行痕迹」——最动人的留存钩子 |
| **反哺偏好**：你这趟实际爱去/避开了啥 → 管家更懂你 | 「越用越准」的数据飞轮 |
| **沉淀为 UGC**：把回忆发出去 → 给别人当攻略（② 论坛） | 喂养 Phase3 社交与专属攻略库 |

---

## 3. 飞轮：三段不是并列，是一个闭环

```
        行前 Plan ──────────────┐
        生成行程 / 导出分享卡      │
            │                    │  下次更准、可复用
            ▼                    │  别人可参考
        行中 Travel              │
        随身看 / 临场调整 / 打卡   │
            │                    │
            ▼                    │
        行后 Remember ───────────┘
        游记 + 点亮地图 + 反哺偏好 + 沉淀 UGC
```

- **留存引擎**：单次 AI 用完就走；这个闭环让用户一趟接一趟回来。
- **数据飞轮**：行中打卡 → 行后沉淀 → 管家更懂你 + 喂养社交地图 → 行前更准。越转越强，**别人无法复制**（它长在用户自己的旅行记忆上）。

---

## 4. 与模块 / 分期对齐（守住 MVP 纪律，别范围爆炸）

生命周期是「时间轴」，三大模块是「能力块」，对应关系：

| 生命周期 | 主要靠哪个模块 | 落在哪个 Phase |
|----------|----------------|----------------|
| 行前 Plan | ① 行程规划 | **Phase 1** |
| 行后 Remember | ③ 记忆地图 + 游记 | Phase 2 |
| 行中 Travel | ① 延伸 + 实时地图/周边 + 语音陪伴（需小程序/App、定位、麦克风） | Phase 2 起 |
| 行后→社交 | ② 论坛 + ③ 升级为社交地图 | Phase 3 |

**纪律不变**（对齐 [07](07-product-modules-and-phasing.md)、[PRD §3](PRD-v0.1.md)）：
- **Phase 1 仍只交付「行前 ①」并打磨到有灵魂**——导出分享卡同时服务行前晒图与行中随身看，是 Phase1 内的高性价比一环。
- 行中、行后正式进入**产品愿景与路线图**，但不挤进 Phase1 开发。先把行前做到「有人爱用、愿转发」，再顺着飞轮往行中、行后长。

---

## 5. 一句话定位升级

> 旧：一个帮你排好行程的 AI 旅行管家。
> **新：一个陪你走完每一程、还记得你走过哪儿的旅行管家——行前帮你排，行中陪你走，行后替你记。**

这句话里，「陪你走、记得你」就是通用 AI 永远说不出口的那部分。


---
